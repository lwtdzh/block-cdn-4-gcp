# block-cdn-4-gcp


Create a firewall rule, for each of ipv4 && ipv6 ip list. To block the GCP to engress data to CDN servers (Akamai+Cloudflare+Fastly).
Save our standard tier 200GB per month free data limit ~!
